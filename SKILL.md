---
name: paper-to-cn-slides
description: 当用户提供英文科研论文（PDF、图片或文本）并要求制作讲解幻灯片、汇报 PPT、组会 slides 时使用。从论文中抽取研究问题、贡献、方法、实验与局限，…把正文与补充材料（如 SI、Appendix、Supplementary 等）的所有 Figure 按原文编号顺序提取、裁剪、压缩为 webp 并保存到 assets/ 目录，HTML 通过相对路径引用，生成正文为中文、专业术语保留英文的 HTML 幻灯片（HTML + assets/ 一起分发）。
---

# 英文论文转中文讲解幻灯片

## 何时使用

- 用户上传或粘贴英文论文，并说“做成 PPT”“生成讲解幻灯片”“帮我准备组会汇报”“这篇 paper 要怎么讲”
- 用户要求把某篇论文整理成便于展示的形式
- 用户提供了论文的补充材料、arXiv 预印本或会议论文全文

不适用：仅要求翻译、仅要求总结摘要、要求制作非展示类文档（如综述 word）。

## 输入

必需：论文全文（PDF、截图或纯文本）。**PDF 形式时默认需要提取其中所有 Figure。**

`src/` 下可以放 1 份、2 份或更多 PDF：

- 只有 1 份：当作完整论文处理
- 有 2 份：默认第一份是正文，第二份是补充材料（如 SI、Appendix 等）；若文件名或内容判断不成立，先向用户确认
- 有 3 份及以上：先列出所有文件名，向用户确认哪份是正文、哪些是补充材料

**补充材料识别原则（防御性指令）**：
当有多个 PDF 时，不要仅凭文件名判断。应优先读取文件头部标题或第一页内容。
- 如果第二份 PDF 的标题包含 "Supplementary"、"Supporting Information"、"Appendix"，或正文末尾引用了 "See SI/Appendix"，则将其视为补充材料。
- 若文件名和内容都无法明确区分，必须询问用户，不得强行归类。

如果 PDF 不在 `src/` 下，先复制或移动到 `src/` 再运行脚本。

可选：汇报时长（默认 30 分钟）、听众背景（默认同领域研究生）、要讲哪些图（默认全部 Figure）、汇报重点。

只拿到摘要或部分页面时：先说明“当前仅有摘要，无法覆盖方法与实验细节”，询问能否提供全文；若用户坚持，生成精简版并在封面标注“基于摘要”。

## 执行顺序（强制）

严格分两阶段，不允许同一次回复输出两份文件：

- **阶段一**：读取论文 → 提取图片 → 生成 `paper-brief.md` → **停下等确认**
- **阶段二**：用户确认后 → 规划骨架 → 生成 `paper-slides.html`

阶段一结束时明确询问：“brief 是否 OK？确认后我生成 slides。”

用户若在阶段一就催“直接出 slides”，仍然先出 brief，并说明：brief 是后续幻灯片的事实依据，先确认可避免 HTML 返工。

## 第一步：抽取结构化信息

先产出 `paper-brief.md`，字段如下：

- **meta**：标题（保留英文）、作者、年份、发表会议或期刊、所属方向
- **problem**：要解决的问题，一句话
- **gap**：已有方法不足，至少说清“差在哪个环节”
- **contributions**：2–4 条，逐条对应原文位置
- **method**：整体框架（输入、输出、关键机制）+ 2–3 个非平凡设计点
- **experiments**：数据集、评价指标、baseline 列表
- **main_results**：具体数值，附原文图表编号
- **ablation**：消融实验支持的结论，指明哪个模块贡献主要提升
- **limitations**：作者承认的局限 + 你的额外观察，两部分分开写
- **glossary**：术语表，格式 `英文术语 | 中文处理方式 | 首次出现位置`
- **figures**：图片对照表，格式见下

原文未明确给出的内容一律写“原文未明确”，不得推测。

## 第一步附：提取图片

运行 `scripts/extract_figures.py`。脚本自动扫描 `src/` 目录下的**所有 PDF**（一份或多份均可），逐份提取 Figure 到 `figs/` 目录，文件名前缀取 PDF 的文件名 stem，用于区分来源。

执行方式：

```bash
python scripts/extract_figures.py

在 `paper-brief.md` 的 `figures` 字段输出对照表：

| 顺序 | 图号 | 来源 PDF | PDF 页码 | 文件名 | 类型 |
|------|------|---------|---------|--------|------|
| 1 | Figure 1 | 论文正文.pdf | p2 | figs/论文正文_p2_img1.png | 位图 |
| 2 | Figure 2a | 论文正文.pdf | p3 | figs/论文正文_p3_img2.png | 位图 |
| 3 | Figure S1 | 补充材料.pdf | p4 | figs/补充材料_p4_fig.png | 矢量 |

“注：补充材料的文件名可能多种多样（SI、Appendix、Supplementary 等），以实际 src/ 下的文件名为准，图中仅作示例。”

提取失败或裁剪异常的图，在对照表里明确标注原因，不要掩盖。

## 第一步附：图的顺序（强制）

所有图按**原文 Figure 编号升序**排列：

1. 正文：Figure 1 → Figure 2 → … → Figure N
2. 补充材料：按其在补充材料中的 Figure 编号升序（如 Figure S1/S2、Figure A1/A2、Figure Sup1 等）
3. 正文图先，补充材料图后
4. 同一张 Figure 的子图（a/b/c/d）放**同一页**，内部按 a → b → c → d 排列，不拆页
5. 正文重复引用同一张 Figure 时，只在首次出现位置讲一次

判断依据：以 **Figure 编号**为准，不以 PDF 页码为准。补充材料图号可能带有 S、A、Sup 等前缀，请自动识别并按其原本的数字顺序排序，编号与页码不一致时按编号排，并在 figures 表里注明。

## 第二步：确定页面骨架

默认 **30 分钟、26–30 页**。骨架围绕图组织。

顺序（不可打乱）：

1. 封面：标题、作者、出处、汇报人、日期
2. 一句话总结
3. 研究背景与问题（1–2 页）
4. 前人工作的不足（1 页）
5. 核心贡献（1 页）
6. 方法整体框架（1 页）
7. **正文 Figure 1 → Figure N**：按编号升序，每张 1–2 页
8. **补充材料 Figure（如 S1、A1 等）**：按编号升序，每张 1 页
9. 实验设置汇总（1 页）
10. 主结果表格（1 页）
11. 消融分析（1–2 页）
12. 局限与思考（1–2 页）
13. 结论与启发（1 页）

页数由内容决定。例如，正文 4 张图 + 补充材料 11 张图时，第 7 步约 8 页、第 8 步约 11 页。

### 讲图页要求

- 图占页面高度 **55–65%**
- 标注**真实图号**（如“Figure 2a”“Figure S7”），不用占位符
- 下方或右侧写 1–3 条讲解，每条一句话
- 列出图里的**关键数值**
- 标题必须是结论句。“引入 NaBH₄ 比例 x 后，1T 相含量从 0% 升到 55%” 合格，“Figure 2a” 不合格

### 时间预算

- 讲图页：每页 1–1.5 分钟
- 其他页：每页 30–60 秒
- 页数偏多时压缩背景页，不压缩讲图页

## 第三步：生成 HTML

- HTML 只有一份，CSS 与 JS 全部内联，不依赖外部网络资源
- 图片**不得 base64 内嵌**，一律以相对路径引用 `assets/` 目录下的 webp 文件
- 16:9 比例，每页一个 `<section class="slide">`
- 方向键左右翻页，右下角页码，支持点击导航
- 页脚标注论文来源
- 视觉风格：白底深字，标题用强调色，正文 22px 左右，表格有边框

图片处理：

- 从 `figs/` 读取，**按 figures 表的“顺序”列遍历**
- 输出目录为 `assets/`，文件名沿用 figs 表里的顺序号：`assets/fig1.webp`、`assets/fig2.webp`、…
- 单张图长边缩放到 **1600 px**（等比，不拉伸），统一转 **webp**，质量 88
- 若某张图文字密集、转 webp 后发虚，允许改用无损 webp 或保留 png，但文件名仍为 `assets/figN.*`
- HTML 里一律写成：`<img src="assets/fig1.webp" alt="Figure 1">`，alt 用真实图号
- **生成后自检：在 HTML 里搜索不到 `data:image`**
- 提取失败的图才用占位样式，写明原因，不得自行绘制
- 交付物是 `paper-slides.html` + `assets/` 文件夹，二者必须一起分发（HTML 不能单独发）

## 手工修补流程

1. 打开生成的 HTML，逐页检查每张图
2. 有问题的图，去 `figs/` 找到对应 PNG，手动重裁
3. 覆盖 `figs/` 里的文件
4. 对 Pi 说：“`figs/xxx.png` 已更新，请重新生成 HTML”
5. Pi 只重新编码 `assets/` 里的 webp、重生成 HTML，**不重新读 PDF、不重新抽取 brief**

如果手动改 HTML 更省事，也可以直接编辑 `paper-slides.html`，但下次重新生成会覆盖。手工改到底就另存为 `paper-slides-final.html`。

## 中英文处理规则

读取 `references/term-rules.md`，严格按其中 7 条规则处理术语。

## 输出格式

- `paper-brief.md`：结构化抽取结果 + 图片对照表
- `figs/`：提取出的所有 Figure 原始图（PNG，作为中间产物）
- `assets/`：webp 版的 Figure，供 HTML 引用（最终产物）
- `paper-slides.html`：幻灯片主文件，通过相对路径引用 `assets/`

分发说明：把 `paper-slides.html` 和 `assets/` 一起打包（zip）或一起放到同一目录。直接单发 HTML 会丢图。

## 约束

- 不得编造数字、图表编号、作者结论、数据集名称
- 不得把原文结论与你的分析混在一起，分析类内容标注“分析：”
- 不得整段复制英文原文作为正文
- 不得用占位符代替可提取的图；只有确实失败才用占位，并写明原因
- 图的顺序必须按 Figure 编号升序，正文图在前、补充材料图在后，子图不拆页
- 论文缺少消融或局限时明确指出缺失，不略过
- 若用户提供的是中文论文，提示本技能面向英文论文，询问是否继续
- HTML 中不得出现 `data:image`。图片一律走 `assets/*.webp` 相对路径。生成后必须自检一次。